_base_: base.yaml

dataset:
  name: iu_xray
  modality: chest_xray
  split:
    train: 0.7
    validation: 0.1
    test: 0.2
  split_unit: uid
  target_fields:
    - impression
    - findings
  target_separator: " "
  prompt_count: 1000
  paired_10_count: 233
  primary_usable_samples: 3331
  expected_split_counts:
    train: 2332
    validation: 333
    test: 666
  preserve_all_views: true
  validation_expectations:
    primary_usable_samples: 3331

_evidence:
  dataset.name: IMPLEMENTATION-ASSUMPTION
  dataset.modality: PAPER-STATED
  dataset.split: PAPER-STATED
  dataset.split_unit: IMPLEMENTATION-ASSUMPTION
  dataset.target_fields: PAPER-STATED
  dataset.target_separator: IMPLEMENTATION-ASSUMPTION
  dataset.prompt_count: PAPER-STATED
  dataset.paired_10_count: IMPLEMENTATION-ASSUMPTION
  dataset.primary_usable_samples: IMPLEMENTATION-ASSUMPTION
  dataset.expected_split_counts: IMPLEMENTATION-ASSUMPTION
  dataset.preserve_all_views: IMPLEMENTATION-ASSUMPTION
  dataset.validation_expectations: IMPLEMENTATION-ASSUMPTION
